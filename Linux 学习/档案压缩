1.压缩格式
	-- *.Z				compress程序压缩的档案
	-- *.bz2			bzip2 程序压缩的档案
	-- *.gz				gzip 程序压缩的档案
	-- *.tar 			tar 程序打包的数据,并没有压缩过
	-- *.tar.gz 		tar程序打包的数据，并且经过gzip压缩


=====gzip=======
压缩 gzip （gzip -cdt# 文档名字）
	-- 参数
		-c 将压缩数据输出到屏幕